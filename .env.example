# Base URL aplikasi
BASE_URL=http://localhost:3000

# Credentials user yang valid (untuk login test)
VALID_EMAIL=admin@playwright.local
VALID_PASSWORD=Password123!

# Credentials user yang tidak valid
INVALID_EMAIL=wrong@example.com
INVALID_PASSWORD=wrongpassword

# Timeout (ms)
DEFAULT_TIMEOUT=30000
